/**
 * Milliseconds per second for shared timer and timeline conversions.
 */
export const MS_PER_SECOND = 1000;

/**
 * Seconds per hour for shared retention calculations.
 */
export const SECONDS_PER_HOUR = 3600;

/**
 * MIME type shared by the backend API and its extension client.
 */
export const MIME_APPLICATION_JSON = 'application/json';
