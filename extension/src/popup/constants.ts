/**
 * Interval (ms) for polling promo-detection status from the background in
 * the popup toolbar.
 */
export const POPUP_DETECTION_POLL_INTERVAL_MS = 2000;

/**
 * Minimum visible width (seconds) for a promo block bar in the popup UI.
 */
export const MIN_PROMO_BLOCK_WIDTH_SEC = 4;
